package day4;

public class AbstractionDemo {
    public static void main(String[] args) {
        // Abstraction is a process in which we hide the intrernal implementation and show or
        // highlight functionality
        // In java , bu using abstarct class and interface we can achieve abstraction
        // use abstract modifier to declare a class or method as abstract

        // Abstract Class: A class that cannot be instantiated (you cannot create an object directly
        // from it). It may contain both abstract methods (methods without a body) and concrete
        // methods (methods with a body).
        // Abstract Method: A method that is declared in an abstract class but does not have a body.
        // It must be implemented by any subclass of the abstract class.

        // abstraction code in animal class
    }
}
